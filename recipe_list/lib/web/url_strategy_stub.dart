// Non-web stub. Selected by the conditional import in `main.dart`
// when `dart.library.js_interop` is unavailable (iOS / Android /
// desktop), so iOS release builds never link against
// `flutter_web_plugins`. Mirrors the API of `url_strategy_web.dart`.
void enablePathUrlStrategy() {}
